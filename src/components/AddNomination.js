import React from 'react';

const AddNomination = (props) =>{
    return(
        <React.Fragment>
            <button>Add to Nominations</button>
        </React.Fragment>
    );
};

export default AddNomination;